<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Classroom Type</title>
  <style>
    body {
      font-family: Helvetica, sans-serif;
      padding: 20px;
    }
    form {
      display: flex;
      flex-direction: column;
    }
    label, input {
      margin-bottom: 10px;
    }
    input[type="text"] {
      padding: 10px;
      width: 300px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }
    button {
      padding: 10px 15px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>Update Classroom Type</h1>
  <form action="/classroom_types/update/<%= classroomType.id %>" method="POST">
    <label for="id">ID:</label>
    <input type="text" id="id" name="id" value="<%= classroomType.id %>" disabled>

    <label for="name">Name:</label>
    <input type="text" id="name" name="name" value="<%= classroomType.name %>" required>

    <button type="submit">Update</button>
  </form>
</body>
</html>