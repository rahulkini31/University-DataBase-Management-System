<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Student Year Level</title>
    <style>
        /* In-file CSS */
        body {
            font-family: Arial, sans-serif;
        }
        .confirmation {
            margin: 20px;
        }
        button {
            padding: 10px;
            margin: 5px;
        }
    </style>
</head>
<body>
    <div class="confirmation">
        <h1>Delete Student Year Level</h1>
        <p>Are you sure you want to delete the student year level for:</p>
        <p>Student ID: <%= student_year_level.student_id %></p>
        <p>Level ID: <%= student_year_level.level_id %></p>
        <p>School Year ID: <%= student_year_level.school_year_id %></p>
        <form action="/student_year_level/delete/<%= student_year_level.student_id %>/<%= student_year_level.level_id %>/<%= student_year_level.school_year_id %>" method="POST">
            <button type="submit">Yes, Delete</button>
        </form>
        <a href="/student_year_level"><button>No, Go Back</button></a>
    </div>
</body>
</html>