<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
        }
        .container {
            margin-top: 30px;
        }
        h1, h2 {
            color: #343a40;
        }
        .card {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome, <%= studentDetails.given_name %>!</h1> <!-- Changed from username to given_name -->
        <h2>Student Dashboard</h2>
        <p>Your role: <%= role %></p>
        <a href="/logout" class="btn btn-danger">Logout</a>

        <h3>Your Profile</h3>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Profile Information</h5>
                <p class="card-text">ID: <%= studentDetails.id %></p>
                <p class="card-text">Full Name: <%= studentDetails.given_name %> <%= studentDetails.middle_name %> <%= studentDetails.last_name %></p> <!-- Updated to show full name -->
                <p class="card-text">Date of Birth: <%= studentDetails.date_of_birth %></p> <!-- Added date of birth -->
                <p class="card-text">Gender: <%= studentDetails.gender %></p> <!-- Added gender -->
                <p class="card-text">Enrollment Date: <%= studentDetails.enrollment_date %></p> <!-- Added enrollment date -->
                <!-- Add more profile information as needed -->
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>