<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Subject</title>
    <style>
        .confirmation {
            width: 300px;
            margin: 0 auto;
            text-align: center;
        }

        button {
            background-color: #f44336;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin: 10px;
        }

        button:hover {
            background-color: #da190b;
        }

        .cancel-button {
            background-color: #4CAF50;
        }

        .cancel-button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="confirmation">
        <h1>Are you sure you want to delete this subject?</h1>

        <p>ID: <%= subject.id %></p>
        <p>Subject Name: <%= subject.subject_name %></p>

        <form action="/subject/delete/<%= subject.id %>" method="POST">
            <button type="submit">Delete</button>
            <a href="/subject"><button type="button" class="cancel-button">Cancel</button></a>
        </form>
    </div>
</body>
</html>