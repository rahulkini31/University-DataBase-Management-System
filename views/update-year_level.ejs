<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Year Level</title>
    <style>
        body { font-family: Helvetica, sans-serif; }
        form { max-width: 400px; margin: 20px; }
        input { margin-bottom: 10px; padding: 10px; width: 100%; }
        button { padding: 10px; }
    </style>
</head>
<body>
    <h1>Update Year Level</h1>
    <form action="/year_level/update/<%= year_level.id %>" method="POST">
        <input type="text" name="id" value="<%= year_level.id %>" readonly />
        <input type="text" name="level_name" value="<%= year_level.level_name %>" required />
        <input type="number" name="level_order" value="<%= year_level.level_order %>" required />
        <button type="submit">Update</button>
    </form>
    <a href="/year_level"><button>Back to Year Levels</button></a>
</body>
</html>